{
  "name": "night-jatra-ticket",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "node -e \"const fs=require('fs'); const dir='.next'; if (fs.existsSync(dir)) fs.rmSync(dir,{recursive:true,force:true}); console.log('Cleared .next cache before dev server');\" && next dev",
    "build": "next build",
    "start": "next start",
    "lint": "next lint",
    "capacitor:sync": "next build && npx cap sync android",
    "capacitor:open": "npx cap open android"
  },
  "dependencies": {
    "@capacitor/android": "^8.5.2",
    "@capacitor/core": "^8.5.2",
    "@capacitor/geolocation": "^8.2.2",
    "firebase": "^12.17.1",
    "next": "^16.3.1",
    "react": "^19.2.8",
    "react-dom": "^19.2.8"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.5.2",
    "@tailwindcss/postcss": "^4.0.0",
    "tailwindcss": "^4.0.0"
  }
}
